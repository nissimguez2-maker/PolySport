# Railway build config shared by all services in this repo.
# Per-service start commands live in the environment config (set via
# `railway environment edit --service-config ... deploy.startCommand ...`),
# NOT here, because a repo-level startCommand would apply to every service
# that shares this repo (logger AND dashboard).

[build]
builder = "NIXPACKS"
